{
  "name": "zk-membership-verifier",
  "version": "1.0.0",
  "description": "Circom and SnarkJS ZK-Proof membership toolkit",
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.0"
  },
  "scripts": {
    "compile": "circom membership.circom --r1cs --wasm --sym",
    "setup": "snarkjs groth16 setup membership.r1cs pot12_final.ptau membership_0000.zkey"
  }
}
